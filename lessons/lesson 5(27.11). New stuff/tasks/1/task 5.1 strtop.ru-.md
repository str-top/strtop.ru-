### Отчёт по проверке решения

**Описание задания:**  
create a python script that writes the multiplication table

**Результат проверки:**  
Неправильно.  

**Комментарии:**  
Решение не содержит Python-скрипта. Вместо кода предоставлен путь к файлу и бессмысленный текст ("asdf").  
**Рекомендации:**  
Напишите скрипт на Python, который выводит таблицу умножения (например, от 1 до 10). Пример:  
```python
for i in range(1, 11):
    for j in range(1, 11):
        print(f"{i}*{j}={i*j}", end="\t")
    print()
```
Передайте только код скрипта в следующем файле.
